const usuarioModel = require('../models/Usuario');

const registerUser = async (req, res) => {
    try {
        
    } catch (error) {
        console.log(error);
    }
}

module.exports({
    registerUser
});